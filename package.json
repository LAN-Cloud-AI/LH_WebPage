{
  "name": "leadshunter-webpage",
  "private": true,
  "version": "2.1.0",
  "type": "module",
  "description": "线索猎手产品官网",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/LAN-Cloud-AI/LH_WebPage.git"
  },
  "homepage": "https://leadshunter.lancloudtech.com/",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build && node scripts/generate-locale-html.mjs",
    "seo:verify": "node scripts/verify-seo.mjs",
    "preview": "vite preview",
    "typecheck": "tsc -b --noEmit",
    "theme:verify": "node --test scripts/theme.test.mjs",
    "deploy": "npm run build && mkdir -p dist/functions && cp functions/_middleware.js dist/functions/ && npx wrangler pages deploy dist --project-name leadshunter-webpage --branch main --commit-dirty=true",
    "guide:render": "node scripts/product-guide/render-html.mjs",
    "guide:pdf": "node scripts/product-guide/export-pdf.mjs",
    "guide:build": "node scripts/product-guide/build.mjs",
    "guide:pack": "npm run guide:render && npm run guide:pdf && npm run guide:build",
    "deploy:guide": "npm run guide:pack && mkdir -p dist-guide/functions && cp functions/_middleware.js dist-guide/functions/ && npx wrangler pages deploy dist-guide --project-name leadshunter-guide --branch main --commit-dirty=true",
    "contact:build": "node scripts/contact-sales/build.mjs",
    "deploy:contact": "npm run contact:build && mkdir -p dist-contact/functions && cp functions/_middleware.js dist-contact/functions/ && npx wrangler pages deploy dist-contact --project-name leadshunter-contact --branch main --commit-dirty=true",
    "dns": "node scripts/cf-dns-leadshunter.mjs",
    "dns:guide": "CF_PAGES_HOST=leadshunter-guide.lancloudtech.com CF_PAGES_PROJECT=leadshunter-guide node scripts/cf-dns-leadshunter.mjs",
    "dns:contact": "CF_PAGES_HOST=leadshunter-contact.lancloudtech.com CF_PAGES_PROJECT=leadshunter-contact node scripts/cf-dns-leadshunter.mjs",
    "check": "npm run build && npm run theme:verify && npm run guide:pack && npm run contact:build && npm run seo:verify"
  },
  "dependencies": {
    "lenis": "^1.3.26",
    "marked": "^18.0.12",
    "motion": "^13.1.1",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "@types/node": "^26.4.0",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.5",
    "@vitejs/plugin-react": "^6.1.0",
    "tailwindcss": "^4.3.3",
    "typescript": "^5.9.3",
    "vite": "^8.2.2"
  }
}
